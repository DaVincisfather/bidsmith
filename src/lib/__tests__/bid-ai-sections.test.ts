// generateAiSection was removed in Task 11 (replaced by per-format AI builders
// called through the planner-driven buildSection dispatcher).
// Tests for the new flow live in bid-planner.test.ts (generateAllSections suite).
